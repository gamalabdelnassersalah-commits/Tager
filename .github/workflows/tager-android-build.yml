name: Build Tager Android

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'android-app/**'
      - '.github/workflows/tager-android-build.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 36
        run: sdkmanager "platforms;android-36" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Build APK and AAB
        working-directory: android-app
        run: gradle --no-daemon :app:assembleDebug :app:bundleRelease

      - name: Prepare outputs
        run: |
          cp android-app/app/build/outputs/apk/debug/app-debug.apk Tager-v1.1.1.apk
          cp android-app/app/build/outputs/bundle/release/app-release.aab Tager-v1.1.1-unsigned.aab

      - name: Upload installable APK
        uses: actions/upload-artifact@v4
        with:
          name: Tager-APK-v1.1.1
          path: Tager-v1.1.1.apk
          retention-days: 30

      - name: Upload Google Play AAB
        uses: actions/upload-artifact@v4
        with:
          name: Tager-AAB-v1.1.1-unsigned
          path: Tager-v1.1.1-unsigned.aab
          retention-days: 30
